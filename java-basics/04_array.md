# 配列

配列は複数のデータをまとめて扱うための仕組みです。

## サンプルコード

```java
public class ArraySample {
    public static void main(String[] args) {

        int[] numbers = {1, 2, 3, 4, 5};

        System.out.println(numbers[0]);
        System.out.println(numbers[1]);
    }
}